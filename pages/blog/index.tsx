import React from "react";
import Title from "../../components/Title";

type Props = {};

const Blog = (props: Props) => {
  return (
    <div>
      <Title name="Blog" />
    </div>
  );
};

export default Blog;
